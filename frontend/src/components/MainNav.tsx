import { Button } from './ui/button';

const MainNav = () => {
  return (
    <Button variant='ghost' className='font-bold hover:bg-red-500'>
      Log In
    </Button>
  );
};

export default MainNav;
